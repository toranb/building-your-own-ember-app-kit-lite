A simple ember.js app used to build your own EAK-lite


To run the ember.js locally

    npm install

    export PATH="./node_modules/.bin:$PATH"

    grunt local

    ./bin/server

    visit http://localhost:3000 to see the running app
